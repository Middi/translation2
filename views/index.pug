extends layout

block content
    .container.container-main
        .flex
            each val in collections
                .card
                    a(href='/'+val.lang)
                        img(src="/images/"+val.lang.toLowerCase()+".png", alt=val.lang)